const fs = require('fs');
let jsx = fs.readFileSync('converted.jsx', 'utf8');

// Need to attach controlled properties to inputs.
// I will do it by hand or by small script? It's better I do it in BusinessOnboarding.jsx file and use it directly.
